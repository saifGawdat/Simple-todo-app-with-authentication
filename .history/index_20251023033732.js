// return masked string
function maskify(cc) {
    var length = cc.length ;
    var mask = cc.array.forEach(element => {
        map
    });((e) => e.replace(e[length - 4] , "#") )
    return mask ;
}

function countPositivesSumNegatives(input) {4
    if(!input || input.length === 0 ) return[];
    var count = 0 ;
    var sum = 0 ;
    for(let i of input){
        if(i>0) count ++;
        else if (i<0) sum 
    }
    
}
